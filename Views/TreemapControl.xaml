<UserControl x:Class="Folderize.Views.TreemapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="500"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="10,6,10,6" LastChildFill="False">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding CurrentDrillDownNode.Name}"
                           FontWeight="Bold"
                           FontSize="13"
                           Foreground="#FFFFFF" />
                <TextBlock Text="{Binding CurrentDrillDownNode.FormattedSize, StringFormat=' ({0})'}"
                           FontWeight="SemiBold"
                           FontSize="13"
                           Foreground="#38BDF8"
                           Margin="4,0,0,0" />
            </StackPanel>

            <!-- Scale Mode Toggle Button -->
            <Button DockPanel.Dock="Right"
                    Content="{Binding TreemapScaleModeText}"
                    Command="{Binding ToggleTreemapScaleCommand}"
                    Background="#1E293B"
                    Foreground="#38BDF8"
                    BorderBrush="#334155"
                    BorderThickness="1"
                    Padding="8,2"
                    FontSize="11"
                    Margin="0,0,6,0"
                    Cursor="Hand"
                    ToolTip="Büyük klasörleri dengeler; küçük klasörlerin rahat okunmasını sağlar. Gerçek orana geçmek için tıklayın." />

            <!-- Reset / Up Button -->
            <Button DockPanel.Dock="Right"
                    Content="▲ Üst Dizin"
                    Command="{Binding NavigateUpCommand}"
                    Background="#1E293B"
                    Foreground="#94A3B8"
                    BorderBrush="#334155"
                    BorderThickness="1"
                    Padding="8,2"
                    FontSize="11"
                    Cursor="Hand" />
        </DockPanel>

        <!-- Treemap Canvas Area -->
        <Border Grid.Row="1" Background="#0F172A" CornerRadius="6" Margin="6" ClipToBounds="True">
            <Grid x:Name="TreemapContainer" SizeChanged="TreemapContainer_SizeChanged">
                <Canvas x:Name="TreemapCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
